"use strict"; // treat all JS Code as newer version, but now a days it's use everywhere.

// alert(3+3) // It use inside browser, we are use node-js

// Data Types:
let name = "Dibyendu" // string
let age = 26 // number, range 2 to 2^53
let isTrue = true // boolean
let state = null
// null, it'a standalone value, it's a represented of empty value
// undefined => value is not present.
let kk // undefined

// symbols => unique

// object

console.log(typeof isTrue);
console.log(typeof null); // type object
console.log(typeof undefined); // type undefined

